<?xml version="1.0" encoding="utf-8"?>
<odoo>

  <!-- Listvy -->
  <record id="view_uap_encounter_tree" model="ir.ui.view">
    <field name="name">uap.encounter.list</field>
    <field name="model">uap.encounter</field>
    <field name="arch" type="xml">
      <list>
        <field name="encounter_id"/>
        <field name="title_en"/>
        <field name="date_observed"/>
        <field name="country_id"/>
        <field name="encounter_class"/>
        <field name="skywatcher_class" optional="show"/>
        <field name="discourse_level"/>
        <field name="official_response"/>
        <field name="status"/>
        <field name="eth_origin"/>
        <field name="observables_count" string="Obs."/>
        <field name="source_count" string="Sources"/>
        <field name="witness_count" string="Witnesses"/>
      </list>
    </field>
  </record>

  <!-- Formvy -->
  <record id="view_uap_encounter_form" model="ir.ui.view">
    <field name="name">uap.encounter.form</field>
    <field name="model">uap.encounter</field>
    <field name="arch" type="xml">
      <form>
        <sheet>
          <div class="oe_button_box" name="button_box">
            <button name="%(action_uap_source)d"
                    type="action"
                    class="oe_stat_button"
                    icon="fa-book">
              <field name="source_count" widget="statinfo" string="Sources"/>
            </button>
            <button name="%(action_uap_witness)d"
                    type="action"
                    class="oe_stat_button"
                    icon="fa-user">
              <field name="witness_count" widget="statinfo" string="Witnesses"/>
            </button>
            <button name="%(action_uap_verification)d"
                    type="action"
                    class="oe_stat_button"
                    icon="fa-check-circle">
              <field name="verification_count" widget="statinfo" string="Log Entries"/>
            </button>
          </div>

          <group string="Identification">
            <field name="encounter_id" placeholder="SWE_PPXL_0001"/>
            <field name="encounter_guid"/>
            <field name="status"/>
          </group>

          <group string="Location &amp; Time">
            <field name="date_observed"/>
            <field name="country_id"/>
            <field name="location"/>
          </group>

          <group string="Classification">
            <field name="encounter_class"/>
            <field name="discourse_level"/>
            <field name="official_response"/>
          </group>

          <group string="ETH / Observables">
            <group>
              <field name="eth_origin"/>
              <field name="intelligence_indicator"/>
            </group>
            <group string="Five Observables">
              <field name="obs_antigravity" widget="boolean_toggle"/>
              <field name="obs_instant_accel" widget="boolean_toggle"/>
              <field name="obs_hypersonic" widget="boolean_toggle"/>
              <field name="obs_low_observability" widget="boolean_toggle"/>
              <field name="obs_transmedium" widget="boolean_toggle"/>
              <field name="observables_count" readonly="1"/>
            </group>
          </group>

          <group string="Skywatcher Classification">
            <group>
              <field name="skywatcher_class"/>
              <field name="skywatcher_confidence"/>
            </group>
            <group col="1">
              <field name="skywatcher_notes" nolabel="1"
                     placeholder="Motivering: form, beteende, spektralsignatur som stödjer klassen..."/>
            </group>
          </group>

          <notebook>
            <page string="Descriptions">
              <group string="English">
                <field name="title_en" nolabel="1"/>
                <field name="description_en" nolabel="1"/>
              </group>
              <group string="Original">
                <field name="language_original"/>
                <field name="title_original" nolabel="1"/>
                <field name="description_original" nolabel="1"/>
              </group>
              <group string="Swedish">
                <field name="description_sv" nolabel="1"/>
              </group>
            </page>

            <page string="Sources &amp; Witnesses">
              <field name="source_ids">
                <list>
                  <field name="name"/>
                  <field name="source_type"/>
                  <field name="url"/>
                </list>
              </field>
              <field name="witness_ids">
                <list>
                  <field name="name"/>
                  <field name="witness_type"/>
                  <field name="credibility"/>
                </list>
              </field>
            </page>

            <page string="Research Notes">
              <group col="1">
                <field name="research_notes" nolabel="1"
                       placeholder="Tagged notes: [BAK] background, [UTB] education, [SEN] sensitive, [OFF] official, [INT] intelligence, [KRI] critical, [SRC] source"/>
              </group>
            </page>

            <page string="Verification Log">
              <field name="verification_ids" nolabel="1">
                <list create="false">
                  <field name="change_date"/>
                  <field name="changed_by"/>
                  <field name="field_name"/>
                  <field name="original_value"/>
                  <field name="updated_value"/>
                  <field name="verification_status"/>
                </list>
              </field>
            </page>

            <page string="Technical">
              <group>
                <field name="neo4j_node_id"/>
              </group>
            </page>
          </notebook>
        </sheet>
      </form>
    </field>
  </record>

  <!-- Kanban -->
  <record id="view_uap_encounter_kanban" model="ir.ui.view">
    <field name="name">uap.encounter.kanban</field>
    <field name="model">uap.encounter</field>
    <field name="arch" type="xml">
      <kanban default_group_by="status">
        <field name="encounter_id"/>
        <field name="title_en"/>
        <field name="country_id"/>
        <field name="encounter_class"/>
        <field name="skywatcher_class"/>
        <field name="discourse_level"/>
        <field name="status"/>
        <templates>
          <t t-name="card">
            <div class="fw-bold">
              <field name="encounter_id"/>
            </div>
            <div class="text-muted small" t-if="record.title_en.value">
              <field name="title_en"/>
            </div>
            <div class="mt-1">
              <span class="badge rounded-pill text-bg-secondary me-1">
                <field name="country_id"/>
              </span>
              <span class="badge rounded-pill text-bg-primary me-1">
                Class <field name="encounter_class"/>
              </span>
              <span class="badge rounded-pill text-bg-warning me-1" t-if="record.skywatcher_class.value">
                SW <field name="skywatcher_class"/>
              </span>
              <span class="badge rounded-pill text-bg-info">
                DL <field name="discourse_level"/>
              </span>
            </div>
          </t>
        </templates>
      </kanban>
    </field>
  </record>

  <!-- Grafvy -->
  <record id="view_uap_encounter_graph" model="ir.ui.view">
    <field name="name">uap.encounter.graph</field>
    <field name="model">uap.encounter</field>
    <field name="arch" type="xml">
      <graph string="Encounters" type="bar">
        <field name="country_id" type="row"/>
        <field name="encounter_class" type="col"/>
      </graph>
    </field>
  </record>

  <!-- Pivotvy -->
  <record id="view_uap_encounter_pivot" model="ir.ui.view">
    <field name="name">uap.encounter.pivot</field>
    <field name="model">uap.encounter</field>
    <field name="arch" type="xml">
      <pivot string="Encounters">
        <field name="skywatcher_class" type="row"/>
        <field name="encounter_class" type="col"/>
      </pivot>
    </field>
  </record>

  <!-- Sökvy -->
  <record id="view_uap_encounter_search" model="ir.ui.view">
    <field name="name">uap.encounter.search</field>
    <field name="model">uap.encounter</field>
    <field name="arch" type="xml">
      <search>
        <field name="encounter_id"/>
        <field name="title_en"/>
        <field name="country_id"/>
        <separator/>
        <filter string="Pending" name="pending" domain="[('status','=','pending')]"/>
        <filter string="Verified" name="verified" domain="[('status','=','verified')]"/>
        <filter string="Archived" name="archived" domain="[('status','=','archived')]"/>
        <separator/>
        <filter string="Class 3+" name="class3" domain="[('encounter_class','in',['3','4'])]"/>
        <filter string="Discourse 4+" name="disc4" domain="[('discourse_level','in',['4','5'])]"/>
        <separator/>
        <filter string="ETH 3+" name="eth3" domain="[('eth_origin','in',['3','4','5'])]"/>
        <filter string="ETH 4+" name="eth4" domain="[('eth_origin','in',['4','5'])]"/>
        <filter string="Intelligence 2+" name="intel2" domain="[('intelligence_indicator','in',['2','3'])]"/>
        <filter string="Has Observables" name="has_obs" domain="[('observables_count','>',0)]"/>
        <filter string="3+ Observables" name="obs3" domain="[('observables_count','>=',3)]"/>
        <separator/>
        <filter string="SW klassificerad" name="sw_set" domain="[('skywatcher_class','!=',False)]"/>
        <filter string="SW ej klassificerad" name="sw_unset" domain="[('skywatcher_class','=',False)]"/>
        <filter string="SW hög konfidens" name="sw_high" domain="[('skywatcher_confidence','in',['auto_high','manual'])]"/>
        <group>
          <filter string="Country" name="by_country" context="{'group_by': 'country_id'}"/>
          <filter string="Encounter Class" name="by_class" context="{'group_by': 'encounter_class'}"/>
          <filter string="Skywatcher Class" name="by_sw" context="{'group_by': 'skywatcher_class'}"/>
          <filter string="Discourse Level" name="by_disc" context="{'group_by': 'discourse_level'}"/>
          <filter string="ETH Origin" name="by_eth" context="{'group_by': 'eth_origin'}"/>
          <filter string="Status" name="by_status" context="{'group_by': 'status'}"/>
        </group>
      </search>
    </field>
  </record>

  <!-- Action -->
  <record id="action_uap_encounter" model="ir.actions.act_window">
    <field name="name">Encounters</field>
    <field name="res_model">uap.encounter</field>
    <field name="view_mode">list,kanban,graph,pivot,form</field>
    <field name="search_view_id" ref="view_uap_encounter_search"/>
  </record>

</odoo>
